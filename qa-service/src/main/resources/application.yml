server:
  port: ${SERVER_PORT:8004}

spring:
  application:
    name: qa-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true